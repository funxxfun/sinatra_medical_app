<h1><%= "#{@user.name}の投稿一覧" %></h1>

<table>
  <tr>
    <th>投稿id</th>
    <th>タイトル</th>
  </tr>
  <% @user.posts.each do |post| %>
    <tr>
      <td><%= post.id %></td>
      <td><a href="/post/<%= post.id %>>"><%= post.title%></a></td>
    </tr>
  <% end %>
</table>